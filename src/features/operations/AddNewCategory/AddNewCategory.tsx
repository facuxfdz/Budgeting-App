import React from 'react'

export interface IAddNewCategoryProps {};

const AddNewCategory: React.FC<IAddNewCategoryProps> = (props) => {
    return (
        <div>
            Add new category
        </div>
    );
}

export default AddNewCategory;