import React from 'react'

export interface IAddNewOperationProps {};

const AddNewOperation: React.FC<IAddNewOperationProps> = (props) => {
    return (
        <div>
            Add new operation
        </div>
    );
}

export default AddNewOperation;